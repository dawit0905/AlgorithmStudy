def checknode(node):
    if(promising(node)):
        if(node에 해답이있으면):
            해당을출력
        else :
            for (node의 모든 자식노드 u에 대해서):
                checknode(node)

def promising(node):
    pass